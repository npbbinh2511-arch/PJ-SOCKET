#include "hftp/server/server.h"

int main() {
    // TODO(B): Parse a control port and server-root configuration, construct concrete
    // dependencies, start Server, wait for shutdown, then join managed workers.
    // Tests: missing/invalid configuration and graceful console shutdown.
    return 0;
}
