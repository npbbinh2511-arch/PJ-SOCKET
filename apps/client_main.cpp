#include "hftp/client/client.h"

int main() {
    // TODO(B/C): Parse server endpoint, connect Client, run a CLI command loop, and
    // display replies/transfer progress without placing protocol logic in the UI.
    // Tests: invalid endpoint, EOF/QUIT, connection failure, visible status changes.
    return 0;
}
